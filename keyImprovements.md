 On import students, make the file field visible for an efficient UX
 Make plans easily accessible via the side menu, just in case the user wants to switch plans, it can be easy for them
 Showing a single student details, not in the form. This should be when a student is tapped from the list, no showing him in a form for editing
 Bring back gender on the student upload template
 Add teachers link on the menu, remove it from students'
 Re-design the admin/teachers  UI
 Re-design the admin/student UI
 Report card template refactor on open full preview
 Refactor the url for admin/health. Now it points to admin/students when you tap on health
 Remove Message Students under admin/messages as it's unnecessary, instead, replace it with Message Parents
 Refactor full admin/events so the user accessed full CRUD
 Bring back Terms nav link on the side bar in case the user wants to create/edit/delete terms
 Give a homogeneous password to teachers uploaded from the list as password, this' editable by the user

 ## DB
 - Move stream from standardlink table to class table for easy lookup
## Teacher's dashboard

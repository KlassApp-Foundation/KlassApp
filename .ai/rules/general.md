---
paths:
  - '**/*'
---

# General

See `AGENTS.md` in the repo root — the single canonical rules file for this project. This pointer exists only so OpenCode's glob-based auto-attach still fires; the actual rules live there, not here.

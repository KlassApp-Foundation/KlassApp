# KlassApp TODOs

> Local task tracking. Not committed to repo (gitignored).

## High Priority

- [ ] **Dependabot vulnerabilities** — 359 vulns (25 critical) on main branch. Run `composer update` + `npm audit fix`, review remaining manually.
- [ ] **Re-enable CI pipeline** — Uncomment `.github/workflows/klassapp-ci.yml` so tests run on every PR push.

## Medium Priority

- [ ] **Wire Grades event into teacher marks flow** — `Teacher/MarksController@saveExamMarks` needs `MarksUpdated` event dispatch (admin notified) + 100% completion check → `GradesPublished` event (parents notified). Already created: `MarksUpdated` event, `NotifyAdminMarksUpdated` listener, `GradesPublished` event, `SendGradesToWhatsApp` listener, `OutboundWhatsAppService`.
- [ ] **Fix timezone in `.env`** — Already changed to `Africa/Kampala`. Ensure config/app.php also reflects this.
- [ ] **Add WhatsApp env vars to `.env.example`** — Already done.

## Low Priority

- [ ] **Tailwind v1 → v4 upgrade** — High risk, low reward. Admin dashboard uses v1 via npm; landing page & premium templates already use CDN (latest version). Skip unless specific need arises.

---
description: "Require knowledge.md Session Log + PR/merge logging so Current Status never lags merged main."
alwaysApply: true
---

# KlassApp — Knowledge Session Log (PR → Merge)

See `AGENTS.md` in the repo root ("Session workflow") — the single canonical rules file for this project. This pointer exists only so Cursor's always-apply mechanism still fires; the actual content lives there, not here.
